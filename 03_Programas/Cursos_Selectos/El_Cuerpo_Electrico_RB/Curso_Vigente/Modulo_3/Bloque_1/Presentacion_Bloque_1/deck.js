const slides=[...document.querySelectorAll('.slide')],notesPanel=document.querySelector('.notes-panel'),reduced=matchMedia('(prefers-reduced-motion: reduce)'),classNotes=globalThis.CLASS_NOTES||[];
slides.forEach((slide,i)=>{const c=document.createElement('div');c.className='chrome';c.innerHTML=`<span class="section">${slide.dataset.section||'Módulo 3'}</span><span class="brand"><img src="assets/logo-regulacion-bioelectrica.png" alt="Regulación Bioeléctrica"><span>Los ejes de regulación · ${String(i+1).padStart(2,'0')}</span></span>`;slide.prepend(c);slide.querySelectorAll('.fragment').forEach(f=>f.setAttribute('aria-hidden','true'))});
let index=Math.max(0,Math.min(slides.length-1,(Number(location.hash.slice(1))||1)-1)),timer;const fragments=s=>[...s.querySelectorAll('.fragment')];
function renderNotes(i){const x=classNotes[i];if(!x)return '<h3>Sin notas</h3>';return `<div class="note-meta"><b>SLIDE ${String(x.n).padStart(2,'0')}</b><span>${x.time}</span></div><h3>${x.title}</h3><h4>Guion</h4>${x.spoken.map(p=>`<p>${p}</p>`).join('')}${x.question?`<h4>Pregunta al grupo</h4><p>${x.question}</p>`:''}${x.board?`<h4>Pizarrón</h4><p>${x.board}</p>`:''}${x.transition?`<div class="note-transition">→ ${x.transition}</div>`:''}<p class="source">Fuente: ${x.source}</p>`}
function show(n,push=true){const next=Math.max(0,Math.min(slides.length-1,n)),old=index,out=slides.find(s=>s.classList.contains('active'));clearTimeout(timer);slides.forEach(s=>s.classList.remove('leaving','enter-forward','enter-backward','exit-forward','exit-backward'));if(out&&out!==slides[next]){out.classList.remove('active');if(!reduced.matches)out.classList.add('leaving',next<old?'exit-backward':'exit-forward')}index=next;slides[index].classList.add('active');if(old!==index&&!reduced.matches)slides[index].classList.add(index<old?'enter-backward':'enter-forward');slides.forEach((s,i)=>{if(i!==index)fragments(s).forEach(f=>{f.classList.remove('visible');f.setAttribute('aria-hidden','true')})});timer=setTimeout(()=>slides.forEach(s=>s.classList.remove('leaving','enter-forward','enter-backward','exit-forward','exit-backward')),650);document.querySelector('.progress').style.width=`${(index+1)/slides.length*100}%`;document.querySelector('.counter').textContent=`${String(index+1).padStart(2,'0')} / ${String(slides.length).padStart(2,'0')}`;notesPanel.innerHTML=renderNotes(index);if(push)history.replaceState(null,'',`#${index+1}`)}
function next(){const f=fragments(slides[index]).find(x=>!x.classList.contains('visible'));if(f){f.classList.add('visible');f.setAttribute('aria-hidden','false');return}show(index+1)}function prev(){const fs=fragments(slides[index]).filter(x=>x.classList.contains('visible'));if(fs.length){const f=fs.at(-1);f.classList.remove('visible');f.setAttribute('aria-hidden','true');return}show(index-1)}function toggleNotes(){notesPanel.classList.toggle('open')}
document.addEventListener('keydown',e=>{if(['ArrowRight','ArrowDown',' ','PageDown'].includes(e.key)){e.preventDefault();next()}if(['ArrowLeft','ArrowUp','PageUp'].includes(e.key)){e.preventDefault();prev()}if(e.key==='Home')show(0);if(e.key==='End')show(slides.length-1);if(e.key.toLowerCase()==='n')toggleNotes();if(e.key.toLowerCase()==='f')document.fullscreenElement?document.exitFullscreen():document.documentElement.requestFullscreen?.();if(e.key==='Escape')notesPanel.classList.remove('open')});document.addEventListener('click',e=>{if(e.target.closest('.notes-panel,.controls'))return;e.clientX>innerWidth*.52?next():prev()});window.addEventListener('hashchange',()=>show((Number(location.hash.slice(1))||1)-1,false));show(index,false);
